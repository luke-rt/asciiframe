// given frame use conversion method on frame
// return string of ascii characters for that frame
/*
mod utils;
mod to_ascii;
mod to_color_ascii;

use to_ascii::*;
use to_color_ascii::*;

pub fn convert(color: bool) {
    // add enum type for strategy eg to_ascii to_ascii color so people can do converter:;convert(converter::ascii_color)
    println!("{}", color);

}
*/
